/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // Enables dark mode based on the class
  theme: {
    extend: {
      colors: {
        primary: "#1D4ED8", // Example primary color, change as needed
      },
    },
  },
  plugins: [],
};
